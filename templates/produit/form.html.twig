
{{ form_start(form) }}
<div class="row">
    <div class="col-md-6 mb-3">
        {{ form_row(form.nom, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="col-md-6 mb-3">
        {{ form_row(form.prix, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>
<div class="mb-3">
    {{ form_row(form.description, {'attr': {'class': 'form-control', 'rows': 4}}) }}
</div>
<div class="row">
    <div class="col-md-6 mb-3">
        {{ form_row(form.category, {'attr': {'class': 'form-select'}}) }}
    </div>
{#    <div class="col-md-6 mb-3">#}
{#        {{ form_row(form.isActive, {'attr': {'class': 'form-check-input'}}) }}#}
{#    </div>#}
</div>
<div class="mb-3">
    {{ form_row(form.tags, {'attr': {'class': 'form-select'}}) }}
</div>
<button class="btn btn-primary w-100">
    <i class="bi bi-check-circle"></i> {{ button_label|default('Créer') }}
</button>
{{ form_end(form) }}
